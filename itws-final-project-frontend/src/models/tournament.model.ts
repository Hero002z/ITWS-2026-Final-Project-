export interface TournamentModel {
    id: number
    name: string
    location: string
    startDate: string
    endDate: string
    timeControl: string
    createdAt: string
    updatedAt: null | string
}